{% extends 'base.html.twig' %}

{% block title %}Guestentry{% endblock %}

{% block body %}
    <h1>Guestentry</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ guestentry.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ guestentry.title }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ guestentry.image }}</td>
            </tr>
            <tr>
                <th>Created_on</th>
                <td>{{ guestentry.createdOn ? guestentry.createdOn|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ guestentry.status }}</td>
            </tr>
            <tr>
                <th>Modified_on</th>
                <td>{{ guestentry.modifiedOn ? guestentry.modifiedOn|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_guestentry_index') }}" class="btn btn-small">Back to list</a>

    <a href="{{ path('app_guestentry_edit', {'id': guestentry.id}) }}" class="btn btn-small">Edit</a>

    {{ include('guestentry/_delete_form.html.twig') }}
{% endblock %}
